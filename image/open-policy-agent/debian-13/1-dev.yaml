# syntax=dhi.io/build:2-debian13

name: Open Policy Agent 1.x (dev)
image: dhi.io/open-policy-agent
variant: dev
tags:
  - 1-dev
  - 1.15-dev
  - 1.15.1-dev
  - 1-debian13-dev
  - 1.15-debian13-dev
  - 1.15.1-debian13-dev
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2024-12-20"
vars:
  BASE_DIGEST: sha256:74fc43fa240887b8159970e434244039aab0c6efaaa9cf044004cdc22aa2a34d
  BASE_TAG: 20250419-debian13
  COMMIT_SHA: 2120bd8047db7f38d92e4e90205e05f81d1e4187
  GOLANG_REFERENCE: dhi/golang:1.26.1-debian13-dev@sha256:2eaf3936008b4a31091eda26511748c1b872925fa5f674f13fbf5b53e7e777c2
  SEMVER_MAJOR_MINOR_VERSION: "1.15"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.15.1
  VERSION: 1.15.1
contents:
  packages:
    - '!base-files'
    - '!gawk'
    - '!libelogind0'
    - '!original-awk'
    - apt
    - bash
    - ca-certificates
    - coreutils
    - diffutils
    - dpkg
    - findutils
    - grep
    - libc-bin
    - mawk
    - perl-base
    - sed
    - util-linux
  builds:
    - name: open-policy-agent
      uses: dhi.io/golang:1.26.1-debian13-dev@sha256:2eaf3936008b4a31091eda26511748c1b872925fa5f674f13fbf5b53e7e777c2
      work-dir: /go/src/github.com/open-policy-agent
      caches:
        - path: /go/pkg/mod
      contents:
        files:
          - url: git+https://github.com/open-policy-agent/opa.git#v1.15.1
            checksum: 2120bd8047db7f38d92e4e90205e05f81d1e4187
            path: /go/src/github.com/open-policy-agent
            spdx:
              name: open-policy-agent
              version: 1.15.1
              packages:
                - name: open-policy-agent
                  purl: pkg:dhi/open-policy-agent@1.15.1
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: update go dependencies
          uses: go/bump@v1
          with:
            deps:
              - github.com/go-viper/mapstructure/v2@v2.4.0
              - go.opentelemetry.io/otel/sdk@v1.40.0
              - golang.org/x/net@v0.51.0
              - google.golang.org/grpc@v1.79.3
            download: true
        - name: test
          runs: |
            set -eux -o pipefail

            if [ "${target.fips}" = "true" ]; then
              # Use lenient mode for the build-time sanity step so Go can still fetch
              # modules during generate/test; the final FIPS image itself remains in
              # strict fips140=only mode.
              export GODEBUG=fips140=on
              export GOFIPS140=v1.0.0
            fi

            # Mirrors upstream's generate target before running unit tests and build.
            CGO_ENABLED=0 GOFLAGS="-buildmode=exe" go generate
            # Run a focused product-relevant Go unit-test subset during the image build;
            # the full image behavior is validated by the image test suite.
            CGO_ENABLED=0 GOFLAGS="-buildmode=exe" go test ./v1/rego ./v1/topdown ./v1/bundle ./v1/runtime ./v1/server/... ./v1/sdk
        - name: build
          uses: go/build@v1
          with:
            ldflags:
              - -X github.com/open-policy-agent/opa/version.Hostname=dhi
              - -X github.com/open-policy-agent/opa/version.Version=1.15.1
            output: ${target.dir}/usr/bin/opa
            packages: .
      outputs:
        - source: ${target.dir}/usr/bin/opa
          target: /usr/bin/opa
          uid: 0
          gid: 0
          mode: "0755"
        - source: /opt/docker/sbom/open-policy-agent
          target: /opt/docker/sbom/open-policy-agent
          uid: 0
          gid: 0
  artifacts:
    - name: dhi.io/static:20250419-debian13@sha256:74fc43fa240887b8159970e434244039aab0c6efaaa9cf044004cdc22aa2a34d
      includes:
        - bin
        - etc
        - home
        - lib
        - lib64
        - opt
        - sbin
        - tmp
        - usr
        - var
      excludes:
        - etc/group
        - etc/os-release
        - etc/passwd
        - var/lib/dpkg/status
      uid: 0
      gid: 0
accounts:
  root: true
  run-as: root
  users:
    - name: nonroot
      uid: 65532
      gid: 65532
    - name: _apt
      uid: 42
      gid: 65532
  groups:
    - name: nonroot
      gid: 65532
      members:
        - nonroot
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
environment:
  DEBIAN_FRONTEND: noninteractive
annotations:
  org.opencontainers.image.description: A minimal Open Policy Agent image
  org.opencontainers.image.licenses: Apache-2.0
entrypoint:
  - /usr/bin/opa
