# syntax=dhi.io/build:2-debian13

name: Open Cluster Management Addon Manager 1.x
image: dhi.io/open-cluster-management-addon-manager
variant: runtime
tags:
  - "1"
  - "1.2"
  - 1.2.0
  - 1-debian13
  - 1.2-debian13
  - 1.2.0-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2026-01-30"
vars:
  BASE_DIGEST: sha256:74fc43fa240887b8159970e434244039aab0c6efaaa9cf044004cdc22aa2a34d
  BASE_TAG: 20250419-debian13
  COMMIT_SHA: 7323d2047ab4ee8c9e478ded4f9c035f37cea702
  GOLANG_REFERENCE: dhi/golang:1.26.1-debian13-dev@sha256:2eaf3936008b4a31091eda26511748c1b872925fa5f674f13fbf5b53e7e777c2
  SEMVER_MAJOR_MINOR_VERSION: "1.2"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.2.0
  VERSION: 1.2.0
contents:
  packages:
    - '!base-files'
  builds:
    - name: addon
      uses: dhi.io/golang:1.26.1-debian13-dev@sha256:2eaf3936008b4a31091eda26511748c1b872925fa5f674f13fbf5b53e7e777c2
      work-dir: /go/src/github.com/open-cluster-management-io/ocm
      caches:
        - path: /go/pkg/mod
      contents:
        files:
          - url: git+https://github.com/open-cluster-management-io/ocm.git#v1.2.0
            checksum: 7323d2047ab4ee8c9e478ded4f9c035f37cea702
            path: /go/src/github.com/open-cluster-management-io/ocm
            spdx:
              name: open-cluster-management-addon-manager
              version: 1.2.0
              packages:
                - name: ocm
                  purl: pkg:golang/github.com/open-cluster-management-io/ocm@1.2.0
                  license: Apache-2.0
            uid: 0
            gid: 0
      pipeline:
        - name: remove vendor directory
          runs: |
            set -eux -o pipefail

            rm -rf vendor
        - name: patch
          uses: go/bump@v1
          privileged: true
          with:
            deps:
              - google.golang.org/grpc@v1.79.3
              - go.opentelemetry.io/otel/sdk@v1.40.0
            download: true
        - name: re-vendor
          runs: |
            set -eux -o pipefail

            go mod vendor
        - name: test
          privileged: true
          runs: |
            set -eux -o pipefail

            if [ "${target.fips}" = "true" ]; then
              # Some unit tests will actually try to use X25519 which is not allowed in FIPS mode
              export GODEBUG=fips140=on
            fi

            # install integration test deps
            go install sigs.k8s.io/controller-runtime/tools/setup-envtest@latest
            source <($(go env GOPATH)/bin/setup-envtest use 1.31 -p env)

            # skip flaky test (assumes connection refused, but can be timeout)
            CGO_ENABLED=0 go test ./cmd/addon/... ./pkg/addon/... -skip "^TestRunController$"
        - name: build
          uses: go/build@v1
          with:
            ldflags:
              - -X open-cluster-management.io/ocm/pkg/version.versionFromGit=1.2.0
              - -X open-cluster-management.io/ocm/pkg/version.commitFromGit=7323d2047ab4ee8c9e478ded4f9c035f37cea702
            output: ${target.dir}/usr/local/bin/addon
            packages: ./cmd/addon
      outputs:
        - source: ${target.dir}/usr/local/bin/addon
          target: /usr/local/bin/addon
          uid: 0
          gid: 0
        - source: /opt/docker/sbom/open-cluster-management-addon-manager
          target: /opt/docker/sbom/open-cluster-management-addon-manager
          uid: 0
          gid: 0
  artifacts:
    - name: dhi.io/static:20250419-debian13@sha256:74fc43fa240887b8159970e434244039aab0c6efaaa9cf044004cdc22aa2a34d
      includes:
        - bin
        - etc
        - home
        - lib
        - lib64
        - opt
        - sbin
        - tmp
        - usr
        - var
      excludes:
        - etc/group
        - etc/os-release
        - etc/passwd
        - var/lib/dpkg/status
      uid: 0
      gid: 0
accounts:
  run-as: "65532"
  users:
    - name: nonroot
      uid: 65532
      gid: 65532
  groups:
    - name: nonroot
      gid: 65532
      members:
        - nonroot
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
paths:
  - type: symlink
    path: /addon
    uid: 0
    gid: 0
    source: /usr/local/bin/addon
annotations:
  org.opencontainers.image.description: A minimal Open Cluster Management Addon Manager image
  org.opencontainers.image.licenses: Apache-2.0
cmd:
  - /usr/local/bin/addon
