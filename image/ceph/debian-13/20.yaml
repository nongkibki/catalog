# syntax=dhi.io/build:2-debian13

name: Ceph 20.x
image: dhi.io/ceph
variant: runtime
tags:
  - "20"
  - "20.3"
  - 20.3.0
  - 20-debian13
  - 20.3-debian13
  - 20.3.0-debian13
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2025-03-03"
vars:
  COMMIT_SHA: e2acaa5c8579e1ced4d71c7b14e0cd0d59a44fcf
  PYTHON_BUILD_REFERENCE: dhi/python:3.13.12-debian13-dev@sha256:61594a3427b191f6978131c2d94b2d90113e413126f5a17ab9e048ccd3863248
  PYTHON_REFERENCE: dhi/python:3.13.12-debian13@sha256:1d999d1b321a2e703275837099436bdd7cba6bf39e5a9ccb3aff246d02462936
  SEMVER_MAJOR_MINOR_VERSION: "20.3"
  SEMVER_MAJOR_VERSION: "20"
  SEMVER_VERSION: 20.3.0
  VERSION: 20.3.0
contents:
  packages:
    - '!base-files'
    - bash
    - coreutils
    - findutils
    - grep
    - libabsl20240722
    - libaio1t64
    - libbabeltrace1
    - libblkid1
    - libboost-context1.83.0
    - libboost-filesystem1.83.0
    - libboost-program-options1.83.0
    - libboost-thread1.83.0
    - libc6
    - libcap-ng0
    - libcap2
    - libcryptsetup12
    - libcurl3t64-gnutls
    - libcurl4t64
    - libexpat1
    - libfmt10
    - libfuse2t64
    - libgcc-s1
    - libgoogle-perftools4t64
    - libibverbs1
    - libicu76
    - libkeyutils1
    - libldap2
    - liblmdb0
    - liblttng-ust1t64
    - liblua5.3-0
    - liblz4-1
    - libnbd0
    - libncurses6
    - libnuma1
    - liboath0t64
    - librabbitmq4
    - librdkafka1
    - librdmacm1t64
    - libsnappy1v5
    - libsqlite3-0
    - libssl3t64
    - libstdc++6
    - libthrift-0.19.0t64
    - libtinfo6
    - libudev1
    - logrotate
    - sed
  builds:
    - name: ceph
      work-dir: ${source.dir}/ceph
      caches:
        - path: /ccache
      contents:
        packages:
          - build-essential
          - g++
          - gcc
          - cmake
          - ccache
          - ninja-build
          - automake
          - libtool
          - pkg-config
          - bison
          - flex
          - ragel
          - patch
          - cpio
          - gperf
          - ca-certificates
          - curl
          - apt-transport-https
          - git
          - bash
          - coreutils
          - findutils
          - sed
          - grep
          - gzip
          - tar
          - xmlstarlet
          - jq
          - socat
          - iproute2
          - jsonnet
          - libaio-dev
          - libblkid-dev
          - libc-ares-dev
          - libcap-dev
          - libcap-ng-dev
          - libcryptsetup-dev
          - libcunit1-dev
          - libcurl4-openssl-dev
          - libevent-dev
          - libexpat1-dev
          - libffi-dev
          - libfmt-dev
          - libfuse-dev
          - libgnutls28-dev
          - libhwloc-dev
          - libicu-dev
          - libkeyutils-dev
          - libldap2-dev
          - liblz4-dev
          - libncurses-dev
          - libnss3-dev
          - libnuma-dev
          - liboath-dev
          - libpciaccess-dev
          - libsctp-dev
          - libsnappy-dev
          - libsqlite3-dev
          - libssl-dev
          - libudev-dev
          - libxml2-dev
          - libzstd-dev
          - uuid-dev
          - zlib1g-dev
          - libabsl-dev
          - libbabeltrace-dev
          - libcrypto++-dev
          - libgoogle-perftools-dev
          - libibverbs-dev
          - librdmacm-dev
          - liblttng-ust-dev
          - liblua5.3-dev
          - libnbd-dev
          - librabbitmq-dev
          - libre2-dev
          - libutf8proc-dev
          - librdkafka-dev
          - libthrift-dev
          - libyaml-cpp-dev
          - liblmdb-dev
          - libnl-genl-3-dev
          - libxmlsec1
          - libxmlsec1t64-nss
          - libxmlsec1t64-openssl
          - libxmlsec1-dev
          - libdaxctl-dev
          - libndctl-dev
          - libpmem-dev
          - libpmemobj-dev
          - libprotobuf-dev
          - protobuf-compiler
          - nlohmann-json3-dev
          - libxsimd-dev
          - systemd
          - systemtap-sdt-dev
          - uuid-runtime
          - xfslibs-dev
          - nasm
        files:
          - url: git+https://github.com/ceph/ceph.git#v20.3.0
            checksum: e2acaa5c8579e1ced4d71c7b14e0cd0d59a44fcf
            path: ${source.dir}/ceph
            spdx:
              name: ceph
              version: 20.3.0
              packages:
                - name: ceph
                  purl: pkg:dhi/ceph@20.3.0
                  license: LGPL-2.1
            uid: 0
            gid: 0
        artifacts:
          - name: dhi.io/python:3.13.12-debian13-dev@sha256:61594a3427b191f6978131c2d94b2d90113e413126f5a17ab9e048ccd3863248
            includes:
              - opt/**
              - usr/**
            uid: 0
            gid: 0
      pipeline:
        - name: install Python build dependencies
          privileged: true
          runs: |
            set -eux -o pipefail

            # CMake's FindCython.cmake uses ${Python3_EXECUTABLE} -m cython --version
            # The cython3 package installs for system Python, but we use Python artifact
            # Install Cython for the artifact Python so CMake can find it
            /opt/python/bin/python3 -m pip install --no-cache-dir Cython==3.2.4

            # Install sphinx-build for documentation generation (CMake requires it)
            # CMake looks for sphinx-build executable, install sphinx which provides it
            /opt/python/bin/python3 -m pip install --no-cache-dir sphinx

            # Install PyYAML - required by build scripts (y2c.py)
            /opt/python/bin/python3 -m pip install --no-cache-dir PyYAML

            # Install setuptools - provides distutils compatibility shim for Python 3.12+
            # Ceph's Cython build scripts use distutils.core.setup which was removed in Python 3.12
            /opt/python/bin/python3 -m pip install --no-cache-dir setuptools

            /opt/python/bin/python3 -m pip install --no-cache-dir prettytable
        - name: patch opentelemetry to use Debian abseil
          uses: git/patch@v1
          work-dir: ${source.dir}/ceph
          with:
            patch: ${source.dir}/patch/build-opentelemetry-use-system-abseil.patch
        - name: patch opentelemetry jaeger thrift compatibility
          uses: git/patch@v1
          work-dir: ${source.dir}/ceph
          with:
            patch: ${source.dir}/patch/build-opentelemetry-jaeger-thrift-compat.patch
        - name: patch distutils remove install layout deb for ceph 19/20
          uses: git/patch@v1
          work-dir: ${source.dir}/ceph
          with:
            patch: ${source.dir}/patch/build-distutils-remove-install-layout-deb.patch
        - name: configure and create build
          work-dir: ${source.dir}/ceph
          privileged: true
          environment:
            CCACHE_DIR: /ccache
            CCACHE_MAXSIZE: 20G
          runs: |
            set -eux -o pipefail

            # Initialize git submodules like in https://github.com/ceph/ceph/blob/main/do_cmake.sh#L5
            git submodule update --init --recursive --recommend-shallow --progress

            # Add Python artifact bin to PATH so CMake can find sphinx-build
            export PATH="/opt/python/bin:${PATH}"

            # Also create a separate build dir
            mkdir -p build

            # Use RelWithDebInfo for production builds (debug builds can take up to 60GB of disk space)
            # Create a bash array
            ARGS=(
              -GNinja
              -DCMAKE_BUILD_TYPE=Release
              -DWITH_PYTHON3=3.13
              -DPython3_EXECUTABLE=/opt/python/bin/python3
              -DWITH_CCACHE=ON
              -DCMAKE_CXX_COMPILER=g++
              -DCMAKE_C_COMPILER=gcc
              -DCMAKE_C_COMPILER_LAUNCHER=ccache
              -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
              -DSETUPTOOLS_DEB_LAYOUT=OFF
              -DWITH_MGR_DASHBOARD_FRONTEND=OFF
              -DWITH_TESTS=OFF
              -DWITH_MANPAGE=OFF
            )

            # Generate the build files
            cmake -S . -B build "${ARGS[@]}"
        - name: build
          work-dir: ${source.dir}/ceph/build
          privileged: true
          runs: |
            set -eux -o pipefail

            # Add Python artifact bin to PATH for build scripts
            export PATH="/opt/python/bin:${PATH}"

            # Build with ninja using available cores.
            # Some C++ translation units (RGW, Boost.Asio templates) use 4-5 GiB each;
            # ensure the builder has sufficient memory (cloud builder recommended).
            ninja -j"$(nproc)"
        - name: install
          work-dir: ${source.dir}/ceph/build
          runs: |
            set -eux -o pipefail

            export PATH="/opt/python/bin:${PATH}"

            # Install Ceph binaries and libraries
            DESTDIR="${target.dir}" ninja install

            # Copy Python runtime dependencies from build Python into the target.
            # prettytable is required by ceph_daemon.py (imported by the ceph CLI).
            # It was pip-installed into the build Python during the earlier step.
            SITE_PKG="${target.dir}/usr/local/lib/python3.13/site-packages"
            BUILD_SITE="/opt/python/lib/python3.13/site-packages"
            cp -r "${BUILD_SITE}"/prettytable "${SITE_PKG}/"
            cp -r "${BUILD_SITE}"/prettytable-*.dist-info "${SITE_PKG}/"
            cp -r "${BUILD_SITE}"/wcwidth "${SITE_PKG}/"
            cp -r "${BUILD_SITE}"/wcwidth-*.dist-info "${SITE_PKG}/"

            # Rook probes execute ceph with a stripped environment, so wrap the
            # generated script to restore the Python and Ceph library search paths.
            mv "${target.dir}/usr/local/bin/ceph" "${target.dir}/usr/local/bin/ceph.real"
            cat > "${target.dir}/usr/local/bin/ceph-wrapper.sh" << 'EOF'
            #!/bin/sh
            export PYTHONPATH="/usr/local/lib/python3.13/site-packages${PYTHONPATH:+:${PYTHONPATH}}"
            export LD_LIBRARY_PATH="/usr/local/lib:/usr/local/lib/ceph${LD_LIBRARY_PATH:+:${LD_LIBRARY_PATH}}"
            exec /opt/python/bin/python3 /usr/local/bin/ceph.real "$@"
            EOF
            install -m 0755 "${target.dir}/usr/local/bin/ceph-wrapper.sh" "${target.dir}/usr/local/bin/ceph"
            rm "${target.dir}/usr/local/bin/ceph-wrapper.sh"

            # Configure dynamic linker to find Ceph libraries at /usr/local/lib
            mkdir -p "${target.dir}/etc/ld.so.conf.d"
            echo "/usr/local/lib" > "${target.dir}/etc/ld.so.conf.d/ceph.conf"
      paths:
        - type: file
          path: ${source.dir}/patch/19-build-make-boost-url-a-list.patch
          uid: 0
          gid: 0
          source: image/ceph/debian-13/patch/19-build-make-boost-url-a-list.patch
        - type: file
          path: ${source.dir}/patch/19-common-dout-fix-gcc14.patch
          uid: 0
          gid: 0
          source: image/ceph/debian-13/patch/19-common-dout-fix-gcc14.patch
        - type: file
          path: ${source.dir}/patch/19-build-arrow-and-pymodule-debian13-compat.patch
          uid: 0
          gid: 0
          source: image/ceph/debian-13/patch/19-build-arrow-and-pymodule-debian13-compat.patch
        - type: file
          path: ${source.dir}/patch/build-opentelemetry-use-system-abseil.patch
          uid: 0
          gid: 0
          source: image/ceph/debian-13/patch/build-opentelemetry-use-system-abseil.patch
        - type: file
          path: ${source.dir}/patch/build-opentelemetry-jaeger-thrift-compat.patch
          uid: 0
          gid: 0
          source: image/ceph/debian-13/patch/build-opentelemetry-jaeger-thrift-compat.patch
        - type: file
          path: ${source.dir}/patch/build-distutils-remove-install-layout-deb.patch
          uid: 0
          gid: 0
          source: image/ceph/debian-13/patch/build-distutils-remove-install-layout-deb.patch
        - type: file
          path: ${source.dir}/patch/21-build-distutils-remove-install-layout-deb.patch
          uid: 0
          gid: 0
          source: image/ceph/debian-13/patch/21-build-distutils-remove-install-layout-deb.patch
      outputs:
        - source: ${target.dir}/usr/local/bin
          target: /usr/local/bin
          uid: 0
          gid: 0
          mode: "0755"
        - source: ${target.dir}/usr/local/lib
          target: /usr/local/lib
          uid: 0
          gid: 0
        - source: ${target.dir}/usr/local/share/ceph
          target: /usr/local/share/ceph
          uid: 0
          gid: 0
        - source: ${target.dir}/etc/ld.so.conf.d/ceph.conf
          target: /etc/ld.so.conf.d/ceph.conf
          uid: 0
          gid: 0
        - source: /opt/docker/sbom/ceph
          target: /opt/docker/sbom/ceph
          uid: 0
          gid: 0
  artifacts:
    - name: dhi.io/python:3.13.12-debian13@sha256:1d999d1b321a2e703275837099436bdd7cba6bf39e5a9ccb3aff246d02462936
      includes:
        - opt/**
        - usr/**
      uid: 0
      gid: 0
accounts:
  run-as: ceph
  users:
    - name: ceph
      uid: 167
      gid: 167
  groups:
    - name: ceph
      gid: 167
      members:
        - ceph
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
environment:
  LD_LIBRARY_PATH: /usr/local/lib
  PATH: /opt/python/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
  PYTHONPATH: /usr/local/lib/python3.13/site-packages
paths:
  - type: directory
    path: /var/lib/ceph
    uid: 167
    gid: 167
    mode: "0755"
  - type: directory
    path: /var/log/ceph
    uid: 167
    gid: 167
    mode: "0755"
  - type: directory
    path: /etc/ceph
    uid: 167
    gid: 167
    mode: "0755"
  - type: directory
    path: /run/ceph
    uid: 167
    gid: 167
    mode: "0755"
  - type: file
    path: /etc/logrotate.d/ceph
    content: |
      /var/log/ceph/*.log {
          daily
          rotate 7
          compress
          delaycompress
          missingok
          notifempty
          copytruncate
      }
    uid: 0
    gid: 0
    mode: "0644"
annotations:
  org.opencontainers.image.description: A minimal Ceph image
  org.opencontainers.image.licenses: LGPL-2.1
cmd:
  - /bin/bash
