# syntax=dhi.io/build:2-debian13

name: Fluentd 1.19.x
image: dhi.io/fluentd
variant: runtime
tags:
  - "1"
  - "1.19"
  - 1.19.2
  - 1-debian13
  - 1.19-debian13
  - 1.19.2-debian13
platforms:
  - linux/amd64
  - linux/arm64
vars:
  EXPAT_DIGEST: sha256:6bb36827ea9fdd0cb5064de572070fc0c82a1443054c91c60a63a80da0552897
  EXPAT_VERSION: 2.7.5
  GEM_HOME: /usr/local/bundle
  JEMALLOC_VERSION: 5.3.0
  RUBY_DEV_DIGEST: sha256:8fd3475fe5fe817795d43e459607ce138898a8d8987bc2862b7bb77e282053d8
  RUBY_DEV_VERSION: 3.4.9
  RUBY_DIGEST: sha256:c7692d23e073d48b503eb8c7008c5cafeb6e8bc2c94112af097ccfb8d4523adf
  RUBY_VERSION: 3.4.9
  SEMVER_MAJOR_MINOR_VERSION: "1.19"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.19.2
  VERSION: 1.19.2
contents:
  packages:
    - '!libelogind0'
    - '!mawk'
    - '!original-awk'
    - base-files
    - bash
    - ca-certificates
    - coreutils
    - libcrypt1
    - libstdc++6
    - libyaml-0-2
    - tini
  builds:
    - name: jemalloc
      contents:
        packages:
          - bash
          - build-essential
          - coreutils
          - grep
          - libc-bin
          - sed
        files:
          - url: https://github.com/jemalloc/jemalloc/releases/download/5.3.0/jemalloc-5.3.0.tar.bz2
            path: ${source.dir}/jemalloc.tar.bz2
            spdx:
              name: jemalloc
              version: 5.3.0
              packages:
                - name: jemalloc
                  purl: pkg:dhi/jemalloc@5.3.0
                  license: MIT
            uid: 0
            gid: 0
      pipeline:
        - name: install jemalloc
          runs: |
            set -eux -o pipefail

            mkdir -p ${target.dir}/opt/jemalloc
            tar -xjf ${source.dir}/jemalloc.tar.bz2 -C ${target.dir}/opt/jemalloc --strip-components=1
            cd ${target.dir}/opt/jemalloc

            # Don't use MADV_FREE to reduce memory usage and improve stability
            # https://github.com/fluent/fluentd-docker-image/pull/350
            echo "je_cv_madv_free=no" > config.cache

            ncpus=$(getconf _NPROCESSORS_ONLN)
            ncpus=$((ncpus > 6 ? 6 : ncpus))

            ./configure -C
            make -j${ncpus}
      outputs:
        - source: ${target.dir}/opt/jemalloc/lib
          target: /usr/lib/libjemalloc
          uid: 0
          gid: 0
    - name: fluent
      uses: dhi.io/ruby:3.4.9-debian13-dev@sha256:8fd3475fe5fe817795d43e459607ce138898a8d8987bc2862b7bb77e282053d8
      privileged: true
      environment:
        BUNDLE_APP_CONFIG: ${BUNDLE_APP_CONFIG}
        BUNDLE_SILENCE_ROOT_WARNING: ${BUNDLE_SILENCE_ROOT_WARNING}
        GEM_HOME: /usr/local/bundle
        LD_LIBRARY_PATH: ${LD_LIBRARY_PATH}
      contents:
        packages:
          - bash
      pipeline:
        - name: install fluentd gem
          runs: |
            set -eux -o pipefail

            gem install oj
            gem install json
            gem install rexml
            gem install async
            gem install async-http
            gem install fluentd -v 1.19.2 --no-doc
        - name: cleanup
          runs: |
            set -eux -o pipefail

            for f in /usr/local/bundle/bin/*; do
              [[ -f $f && ${f##*/} != fluentd ]] && rm -v $f
            done

            # Ensure fluentd remains!
            fluentd --version
      outputs:
        - source: /usr/local/bin
          target: /usr/local/bin
          uid: 0
          gid: 0
        - source: /usr/local/bundle
          target: /usr/local/bundle
          uid: 0
          gid: 0
        - source: /usr/local/include
          target: /usr/local/include
          uid: 0
          gid: 0
        - source: /opt/docker/sbom
          target: /opt/docker/sbom
          uid: 0
          gid: 0
  artifacts:
    - name: dhi.io/ruby:3.4.9-debian13@sha256:c7692d23e073d48b503eb8c7008c5cafeb6e8bc2c94112af097ccfb8d4523adf
      includes:
        - usr/**
      uid: 0
      gid: 0
accounts:
  run-as: nonroot
  users:
    - name: nonroot
      uid: 65532
      gid: 65532
  groups:
    - name: nonroot
      gid: 65532
      members:
        - nonroot
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "13"
  version-codename: trixie
  pretty-name: Docker Hardened Images/Debian GNU/Linux 13 (trixie)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
environment:
  BUNDLE_APP_CONFIG: /usr/local/bundle
  BUNDLE_SILENCE_ROOT_WARNING: "true"
  GEM_HOME: /usr/local/bundle
  PATH: /usr/local/bundle/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
paths:
  - type: directory
    path: /etc/fluent
    uid: 0
    gid: 0
  - type: directory
    path: /etc/fluent/plugins
    uid: 0
    gid: 0
  - type: directory
    path: /var/log/fluent
    uid: 65532
    gid: 65532
    mode: "0755"
  - type: file
    path: /usr/local/bin/docker-entrypoint.sh
    uid: 0
    gid: 0
    mode: "0755"
    source: image/fluentd/bin/docker-entrypoint.sh
  - type: file
    path: /etc/fluent/fluent.conf
    uid: 0
    gid: 0
    mode: "0755"
    source: image/fluentd/config/fluent.conf
annotations:
  org.opencontainers.image.description: A minimal Fluentd image
  org.opencontainers.image.licenses: Apache-2.0
entrypoint:
  - /usr/bin/tini
  - --
  - /usr/local/bin/docker-entrypoint.sh
cmd:
  - fluentd
ports:
  - 24224/tcp
  - 5140/tcp
