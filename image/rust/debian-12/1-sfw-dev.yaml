# syntax=dhi.io/build:2-debian12

name: Rust 1.x / Socket Firewall (sfw, dev)
image: dhi.io/rust
variant: dev
flavor: sfw
tags:
  - 1-debian12-sfw-dev
  - 1.94-debian12-sfw-dev
  - 1.94.1-debian12-sfw-dev
platforms:
  - linux/amd64
  - linux/arm64
dates:
  release: "2025-09-18"
vars:
  ARCH: '#{ target.arch == "amd64" ? "x86_64" : "aarch64" }'
  BINUTILS_DIGEST: sha256:6c33a7e6bb1b75a710993895f624975e758e11743781455add63ceeeab23d55b
  BINUTILS_VERSION: "2.46"
  SEMVER_MAJOR_MINOR_VERSION: "1.94"
  SEMVER_MAJOR_VERSION: "1"
  SEMVER_VERSION: 1.94.1
  SFW_ARCH: '#{ target.arch == "amd64" ? "x86_64" : "arm64" }'
  SFW_VERSION: 1.6.1
  VERSION: 1.94.1
contents:
  packages:
    - '!binutils'
    - '!binutils-aarch64-linux-gnu'
    - '!binutils-common'
    - '!binutils-x86-64-linux-gnu'
    - '!gawk'
    - '!libbinutils'
    - '!libctf-nobfd0'
    - '!libctf0'
    - '!libelogind0'
    - '!libgprofng0'
    - '!libsframe1'
    - '!make-guile'
    - '!original-awk'
    - apt
    - base-files
    - bash
    - build-essential
    - ca-certificates
    - coreutils
    - diffutils
    - dpkg
    - gcc
    - grep
    - libc-bin
    - mawk
    - perl-base
    - sed
    - util-linux
  builds:
    - name: rust
      contents:
        packages:
          - bash
          - coreutils
          - findutils
          - grep
          - sed
        files:
          - url: https://static.rust-lang.org/dist/rust-1.94.1-${ARCH}-unknown-linux-gnu.tar.xz
            path: ${source.dir}
            spdx:
              name: rust
              version: 1.94.1
              packages:
                - name: rust
                  purl: pkg:dhi/rust@1.94.1
                  license: MIT
                - name: cargo
                  purl: pkg:dhi/cargo@1.94.1
                  license: MIT
            uid: 0
            gid: 0
      pipeline:
        - name: install
          runs: |
            set -eux -o pipefail

            cd "rust-1.94.1-${ARCH}-unknown-linux-gnu"
            ./install.sh --destdir="${target.dir}" --disable-ldconfig

            # remove html documentation
            rm -rf "${target.dir}/usr/local/share/doc/rust"
      outputs:
        - source: ${target.dir}
          target: /
          uid: 0
          gid: 0
        - source: /opt/docker/sbom
          target: /opt/docker/sbom
          uid: 0
          gid: 0
    - name: sfw
      contents:
        packages:
          - bash
          - coreutils
          - findutils
          - libstdc++6
        files:
          - url: https://github.com/SocketDev/sfw-free/releases/download/v1.6.1/sfw-free-linux-${SFW_ARCH}
            path: ${target.dir}/opt/socket/bin/sfw
            uid: 0
            gid: 0
            mode: "0755"
          - url: https://raw.githubusercontent.com/SocketDev/sfw-free/refs/tags/v1.6.1/LICENSE.md
            path: ${target.dir}/opt/socket/LICENSE.md
            uid: 0
            gid: 0
      pipeline:
        - name: create alias
          runs: |
            set -eux -o pipefail

            echo "Creating cargo alias"

            cat > "${target.dir}/opt/socket/bin/cargo" << 'EOF'
            #!/bin/bash

            # if DHI_NO_SFW is set to true, run the command without sfw, otherwise run with sfw
            if [ "${DHI_NO_SFW}" = "true" ]; then
              /usr/local/bin/cargo "$@"
            else
              sfw /usr/local/bin/cargo "$@"
            fi
            EOF

            chmod +x "${target.dir}/opt/socket/bin/cargo"
      outputs:
        - source: ${target.dir}/opt/socket
          target: /opt/socket
          uid: 0
          gid: 0
  artifacts:
    - name: dhi.io/pkg-binutils:2.46-debian12@sha256:6c33a7e6bb1b75a710993895f624975e758e11743781455add63ceeeab23d55b
      excludes:
        - etc/group
        - etc/os-release
        - etc/passwd
        - var/lib/dpkg/status
      uid: 0
      gid: 0
accounts:
  root: true
  run-as: root
  users:
    - name: nonroot
      uid: 65532
      gid: 65532
    - name: _apt
      uid: 42
      gid: 65532
  groups:
    - name: nonroot
      gid: 65532
      members:
        - nonroot
os-release:
  name: Docker Hardened Images (Debian)
  id: debian
  version-id: "12"
  version-codename: bookworm
  pretty-name: Docker Hardened Images/Debian GNU/Linux 12 (bookworm)
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
work-dir: /build
environment:
  DEBIAN_FRONTEND: noninteractive
  PATH: /opt/socket/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
  RUST_VERSION: 1.94.1
annotations:
  org.opencontainers.image.description: A minimal Rust image
  org.opencontainers.image.licenses: MIT
cmd:
  - /bin/bash
