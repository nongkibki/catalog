# syntax=dhi.io/build:2-alpine3.23

image: dhi.io/pkg-dhi-enterprise-conf
variant: runtime
tags:
  - 0.1.0-alpine3.23
  - 0.1.0-r1-alpine3.23
platforms:
  - linux/amd64
  - linux/arm64
vars:
  PACKAGE_NAME: dhi-enterprise-conf
  REL: "1"
  SEMVER_MAJOR_MINOR_VERSION: "0.1"
  SEMVER_MAJOR_VERSION: "0"
  SEMVER_VERSION: 0.1.0
  VERSION: 0.1.0
contents:
  repositories:
    - https://dhi.io/apk/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/main
    - https://dl-cdn.alpinelinux.org/alpine/v3.23/community
  keyring:
    - https://dhi.io/keyring/dhi-apk@docker-0F81AD7700D99184.rsa.pub
  packages:
    - alpine-baselayout-data
  builds:
    - name: conf
      work-dir: ${source.dir}/aports/dhi-enterprise-conf
      contents:
        files:
          - url: https://dhi.io/keyring/dhi-apk-sec@docker-0F81AD7700D99184.rsa.pub
            path: ${target.dir}/etc/apk/keys/dhi-apk-sec@docker-0F81AD7700D99184.rsa.pub
            uid: 0
            gid: 0
      pipeline:
        - name: create post-install script
          runs: |
            set -eux -o pipefail

            cat << EOF > "dhi-enterprise-conf.post-install"
            #!/bin/sh
            echo "configuring DHI enterprise package repository..."
            echo "" >> /etc/apk/repositories
            echo "https://dhi.io/apk/alpine/v3.23/security" >> /etc/apk/repositories
            EOF
        - uses: abuild/setup@v1
        - uses: abuild/apk@v1
          with:
            arch: noarch
            description: DHI enterprise package repository configuration
            install: $pkgname.post-install
            license: DHI
            name: dhi-enterprise-conf
            rel: 1
            version: 0.1.0
        - uses: abuild/build@v1
os-release:
  name: Docker Hardened Images (Alpine)
  id: alpine
  version-id: "3.23"
  pretty-name: Docker Hardened Images/Alpine Linux v3.23
  home-url: https://docker.com/products/hardened-images/
  bug-report-url: https://docker.com/support/
